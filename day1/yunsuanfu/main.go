package main

import "fmt"

func main()  {
	//算术运算符
    //+ - * / %

	//++ --
	num1 := 0
	num1++
	//num2 = num1++  不能赋值 🙅

	fmt.Println(num1)

	//关系运算符
	//强类型语言 相同类型 做判断
	// == != <= >= > <


	//逻辑运算符
	// && || !

	//位运算符
	// & 按位与
	// ^ 异或 按位 异（就是不一样的时候 比如 01-> 1  00 || 11 -> 0） 为 1
	// | 按位或
	// << 左移位
	// >> 右移位

	//赋值运算符
	// += -= *= /= %= <<= >>=   按照字面理解就好

}
